export enum DialogMode {
  ADD = 'add',
  VIEW = 'view',
  EDIT = 'edit',
  DELETE = 'delete',
  CLOSE = 'close',
}

export enum DialogCommand {
  UPDATED = 'updated',
  DELETED = 'deleted',
  ERROR = 'error',
}
